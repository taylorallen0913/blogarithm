{
  "name": "@blogarithm/create-blogarithm-app",
  "version": "0.0.1",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "MIT",
  "bin": {
    "create-blogarithm-app": "dist/index.js"
  },
  "scripts": {
    "start": "node dist/index.js",
    "setup": "yarn && yarn build",
    "build": "yarn clean:dist && tsc",
    "clean": "yarn clean:dist && yarn clean:modules",
    "clean:dist": "rm -rf dist",
    "clean:modules": "rm -rf node_modules"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/taylorallen0913/blogarithm.git"
  },
  "dependencies": {
    "args": "^5.0.1",
    "chalk": "^4.1.0",
    "figlet": "^1.4.0",
    "fs-extra": "^9.0.1",
    "inquirer": "^7.2.0"
  },
  "devDependencies": {
    "@types/args": "^3.0.0",
    "@types/figlet": "^1.2.0",
    "@types/inquirer": "^6.5.0",
    "@types/node": "^14.0.14",
    "typescript": "^3.9.5"
  }
}
