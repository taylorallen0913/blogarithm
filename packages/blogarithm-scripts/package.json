{
  "name": "blogarithm-scripts",
  "version": "0.0.1",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "license": "MIT",
  "bin": {
    "blogarithm-scripts": "dist/index.js"
  },
  "scripts": {
    "setup": "yarn && yarn build",
    "build": "yarn clean:dist && tsc",
    "clean": "yarn clean:dist && yarn clean:modules",
    "clean:dist": "rm -rf dist",
    "clean:modules": "rm -rf node_modules"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/taylorallen0913/blogarithm.git"
  },
  "devDependencies": {
    "@types/node": "^14.0.14",
    "typescript": "^3.9.5"
  },
  "dependencies": {}
}
